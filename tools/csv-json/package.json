{
  "name": "flipcsv",
  "private": true,
  "description": "CSV <-> JSON converter — pure ESM engine + self-contained page. The page inlines src/*; run the suite with npm test.",
  "type": "module",
  "scripts": { "test": "node --test test/engine.test.mjs" }
}
